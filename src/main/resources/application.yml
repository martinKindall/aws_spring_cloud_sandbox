spring:
  application:
    name: myapplication
  security:
    oauth2:
      client:
        provider:
          cognito:
            issuerUri: https://cognito-idp.us-east-1.amazonaws.com/us-east-1_vLvD5n28f

cloud:
  aws:
    stack:
      auto: false

message: justAnyLocalMachine

dynamoUrl: "http://localhost:4566"

serverId: "local"

logging:
  level:
    com:
      codigo_morsa: DEBUG